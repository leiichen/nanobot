# nanobot 快速上手学习路线

> 面向第一次阅读 nanobot 源码的开发者。目标不是一次读完项目，而是先跑通，再看懂一条消息如何完成“接收 → 调模型 → 调工具 → 回复”。

## 1. 先记住这一条主链

```text
Channel → MessageBus → AgentLoop → AgentRunner → Provider
                                      ↓
                                     Tool
                                      ↓
Session ←────────────────────────── 最终结果
```

- `Channel`：接收 Telegram、WebUI、Slack 等外部消息。
- `MessageBus`：在 Channel 和 Agent 之间传递消息。
- `AgentLoop`：负责会话、上下文和一轮消息的整体编排。
- `AgentRunner`：循环调用模型；模型要用工具时执行工具，再把结果交回模型。
- `Provider`：对接具体的大模型服务。
- `Session`：保存对话历史，供下一轮恢复上下文。

理解这条链，就理解了项目的主干。

## 2. 第一步：把项目跑起来（约 30 分钟）

### 环境要求

- Python 3.11+
- 一个可用的模型服务、模型名和 API Key；本地模型可能不需要 Key
- 源码运行 WebUI 时需要 Bun 或 npm

### 安装后端依赖

```powershell
pip install -e ".[dev]"
```

### 构建 WebUI

仓库源码不包含已构建的前端文件，首次运行前执行：

```powershell
cd webui
bun install
bun run build
cd ..
```

没有 Bun 时可使用：

```powershell
cd webui
npm install
npm run build
cd ..
```

### 配置并启动

```powershell
# 首次配置 Provider、模型和 WebUI
nanobot onboard --wizard

# 检查配置
nanobot status

# 启动 Gateway
nanobot gateway
```

浏览器打开 `http://127.0.0.1:8765`，先发送一句 `你好`。

完成标准：

- `nanobot status` 中 Config、Workspace 正常；
- WebUI 能收到模型回复；
- Gateway 终端没有鉴权或模型配置错误。

如果启动失败，先看 [快速安装文档](docs/quick-start.md) 和 [故障排查](docs/troubleshooting.md)，不要急着读源码。

## 3. 第二步：按消息链读代码（约 2 小时）

只读下面几个入口。第一次不要展开所有 Channel、Provider 和 Tool 实现。

### 3.1 消息从哪里来：MessageBus（10 分钟）

先读：

1. [nanobot/bus/events.py](nanobot/bus/events.py)：`InboundMessage` 和 `OutboundMessage` 的字段。
2. [nanobot/bus/queue.py](nanobot/bus/queue.py)：入站、出站两个异步队列。
3. [nanobot/channels/base.py](nanobot/channels/base.py)：搜索 `_handle_message()`，看 Channel 如何调用 `publish_inbound()`。

你只需要回答两个问题：

- 外部消息如何变成 `InboundMessage`？
- Agent 的回复如何通过 `OutboundMessage` 返回 Channel？

### 3.2 一轮消息如何编排：AgentLoop（35 分钟）

打开 [nanobot/agent/loop.py](nanobot/agent/loop.py)，按顺序搜索：

1. `run()`：持续从 MessageBus 取消息。
2. `_dispatch()`：保证同一会话串行、不同会话可并发。
3. `_process_message()`：创建 `TurnContext` 并驱动状态机。
4. `_TRANSITIONS`：查看正常状态流转。
5. `_state_build()`：组装历史、系统提示、记忆和本轮消息。
6. `_state_run()`：把消息交给 `AgentRunner`。
7. `_state_save()`、`_state_respond()`：保存历史并生成出站消息。

正常路径是：

```text
RESTORE → COMPACT → COMMAND → BUILD → RUN → SAVE → RESPOND → DONE
```

第一次阅读时忽略 MCP 热重载、自动化任务、中途消息注入等分支。

### 3.3 模型和工具如何循环：AgentRunner（45 分钟）

打开 [nanobot/agent/runner.py](nanobot/agent/runner.py)，按顺序搜索：

1. `AgentRunSpec`：Loop 传给 Runner 的输入。
2. `AgentRunResult`：Runner 返回给 Loop 的结果。
3. `run()`：生命周期 Hook 和统一异常处理。
4. `_run_core()`：最重要的多轮循环。
5. `_request_model()`：向 Provider 发起一次模型请求。
6. `_execute_tools()`、`_run_tool()`：执行模型请求的工具。

把 `_run_core()` 简化后只有四步：

```python
while 还没达到最大轮数:
    response = 调用模型(messages)
    if response 要求调用工具:
        result = 执行工具(response.tool_calls)
        messages += 工具结果
        continue
    return response.content
```

项目中的真实代码还处理流式输出、重试、上下文压缩、超时和中途注入。先掌握上面的骨架，再看这些保护逻辑。

### 3.4 模型和工具的边界（20 分钟）

读三个小入口：

- [nanobot/providers/base.py](nanobot/providers/base.py)：`LLMResponse`、`ToolCallRequest`、`LLMProvider`。
- [nanobot/agent/tools/base.py](nanobot/agent/tools/base.py)：一个工具需要提供什么。
- [nanobot/agent/tools/registry.py](nanobot/agent/tools/registry.py)：工具如何注册、生成 schema、校验参数和执行。

关键关系：

```text
ToolRegistry.get_definitions() → 把工具 schema 发给模型
模型返回 ToolCallRequest       → ToolRegistry.execute()
工具结果追加到 messages        → 再次请求模型
```

### 3.5 历史如何保存（10 分钟）

打开 [nanobot/session/manager.py](nanobot/session/manager.py)，只看：

- `Session.add_message()`：追加消息；
- `Session.get_history()`：读取上下文；
- `SessionManager.save()`：持久化会话。

先知道“上一轮历史会进入下一轮模型上下文”即可。自动压缩和长期记忆留到后面。

## 4. 第三步：动手验证（约 30 分钟）

不要只阅读，给以下位置打断点或临时加日志：

1. `AgentLoop._dispatch()`：查看 `msg.channel`、`msg.chat_id`、`session_key`。
2. `AgentLoop._state_build()`：查看 `ctx.initial_messages`。
3. `AgentRunner._run_core()`：查看 `response.content` 和 `response.tool_calls`。
4. `ToolRegistry.execute()`：查看工具名、参数和返回值。
5. `AgentLoop._state_save()`：查看最终写入的消息。

用 WebUI 发送两个问题：

```text
你好，请只回复“收到”
列出当前工作区的文件
```

第一个问题用于观察普通回复，第二个用于观察完整的工具调用循环。

## 5. 看懂主链后再选方向

| 目标 | 阅读入口 |
|---|---|
| 新增模型服务 | `providers/base.py` → `providers/registry.py` → `providers/factory.py` |
| 新增工具 | `agent/tools/base.py` → `agent/tools/registry.py` → `agent/tools/filesystem.py` |
| 新增聊天平台 | `channels/base.py` → 一个现有 Channel → `channels/manager.py` |
| 理解上下文 | `agent/context.py` → `agent/autocompact.py` |
| 理解长期记忆 | `agent/memory.py` → `docs/memory.md` |
| 理解 WebUI | `docs/webui.md` → `webui/src/` |
| 调用 Python SDK | `docs/python-sdk.md` → `nanobot/nanobot.py` |

## 6. 暂时不要做的事

- 不要从头通读 `nanobot/cli/commands.py`，它包含大量启动和配置分支。
- 不要一次阅读所有 Provider、Channel 或 Tool；每类选一个具体实现即可。
- 不要一开始研究 Memory、MCP、子代理、定时任务和长期目标。
- 不要为了学习修改 `loop.py`、`runner.py` 的运行逻辑；它们是核心路径。
- 不要全项目执行 `ruff format`，只检查或格式化自己修改的文件。

## 7. 最小验证命令

修改代码后，优先运行与改动最接近的测试：

```powershell
ruff check nanobot/
pytest tests/对应测试文件.py -v
```

WebUI 改动使用：

```powershell
cd webui
bun run test
bun run build
```

## 8. 推荐阅读顺序

```text
docs/quick-start.md
→ bus/events.py + bus/queue.py
→ agent/loop.py
→ agent/runner.py
→ providers/base.py
→ agent/tools/registry.py
→ session/manager.py
→ 按兴趣选择一个子系统
```

一句话复盘：Channel 收消息，Bus 传消息，Loop 编排一轮，Runner 循环调用模型和工具，Session 保存结果，Bus 再把回复交给 Channel。
