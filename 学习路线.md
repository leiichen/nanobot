# 🐈 nanobot 初学者学习路线图

> 适用版本：v0.2.x ｜ Python ≥ 3.11 ｜ 本文面向第一次接触本项目的开发者

---

## 0. 一分钟读懂 nanobot

nanobot 是一个**轻量级开源 AI Agent 框架**（Python 后端 + React/TypeScript WebUI）。
它做的事可以用一句话概括：

> 把聊天平台来的消息，交给 LLM 决策、调用工具、再回复出去——并让这个过程能长期运行、可记忆、可扩展。

主流程只有一条链：

```
聊天平台 ─► MessageBus ─► AgentLoop ─► AgentRunner ─► LLM Provider ─► Tool 执行 ─► Session 存储 ─► 回复
```

只要抓住这条链，整份代码就不会迷路。

---

## 1. 前置知识

不要求精通，能读懂即可：

| 主题 | 最低要求 |
|------|---------|
| Python | 理解 `async/await`、`dataclass`、装饰器、类型注解 |
| Git | `clone` / `branch` / `commit` / `pull request` |
| LLM 基础 | 知道 token、system prompt、tool calling（函数调用） |
| 终端 | `cd` / `ls` / `pip install` |

---

## 2. 快速启动（30 分钟）

先把项目跑起来，亲眼看到它做什么，再回头看代码。

```bash
git clone https://github.com/HKUDS/nanobot.git
cd nanobot

# 安装（含开发依赖）
pip install -e ".[dev]"

# 首次运行会引导你配置一个 LLM Provider（至少一个 API Key）
# 支持 Anthropic / OpenAI / GitHub Copilot / Azure / Bedrock 等

# 启动 Gateway（后台服务 + WebUI 一体）
nanobot gateway

# 浏览器打开 http://localhost:8765 → WebUI 里直接对话
```

> ⚠️ 源码安装不会自动构建前端。若 `http://localhost:8765` 显示 Not Found，需先 `cd webui && npm install && npm run build` 再重启 gateway。

**完成标志**：能在 WebUI 里和 bot 对话，看到它调用工具、给出回复。

---

## 3. 核心概念地图

把下面这张表印在脑子里，它是整个项目的"骨架"：

| 概念 | 作用 | 对应目录 |
|------|------|---------|
| **Channel** | 输入/输出通道（Telegram、微信、Slack…20+ 平台） | [nanobot/channels/](nanobot/channels/) |
| **MessageBus** | 消息总线，解耦 Channel 与 Agent | [nanobot/bus/](nanobot/bus/) |
| **AgentLoop** | 核心编排引擎：收消息→建上下文→调度 | [nanobot/agent/loop.py](nanobot/agent/loop.py) |
| **AgentRunner** | 多轮对话执行器：循环调 LLM、执行工具 | [nanobot/agent/runner.py](nanobot/agent/runner.py) |
| **Provider** | LLM 供应商抽象（Anthropic/OpenAI/Azure/Bedrock…） | [nanobot/providers/](nanobot/providers/) |
| **Tool** | 暴露给 LLM 的能力（文件/Shell/搜索/MCP/子Agent…） | [nanobot/agent/tools/](nanobot/agent/tools/) |
| **Session** | 会话历史与状态管理、自动压缩 | [nanobot/session/](nanobot/session/) |
| **Memory** | 长期记忆的存储与召回（Dream 两阶段整合） | [nanobot/agent/memory.py](nanobot/agent/memory.py) |
| **Config** | Pydantic 配置 schema，从 `~/.nanobot/config.json` 加载 | [nanobot/config/](nanobot/config/) |
| **WebUI** | React 前端，经 WebSocket 多路复用协议连 Gateway | [webui/](webui/) |

---

## 4. 目录速查

```
nanobot/
├── nanobot/                  # Python 主包（核心代码）
│   ├── cli/                  # 命令行入口（gateway / serve / config…）
│   ├── agent/                # 🔥 核心：Loop / Runner / 工具 / 记忆
│   │   ├── loop.py           #    AgentLoop —— 编排核心
│   │   ├── runner.py         #    AgentRunner —— LLM 对话循环
│   │   ├── context.py        #    上下文组装
│   │   ├── autocompact.py    #    上下文自动压缩
│   │   ├── memory.py         #    记忆与 Dream 整合
│   │   ├── subagent.py       #    子代理
│   │   ├── cron_turns.py     #    定时任务调度
│   │   └── tools/            #    工具实现（见 §5 第 7 站）
│   ├── channels/             # 聊天平台接入（20+ 平台）
│   ├── providers/            # LLM 供应商（见 §5 第 6 站）
│   ├── config/               # Pydantic 配置 schema + 加载器
│   ├── session/              # 会话管理、历史压缩、目标状态
│   ├── bus/                  # 消息总线 + 事件系统
│   ├── command/              # 斜杠命令路由
│   ├── api/                  # OpenAI 兼容 HTTP API
│   ├── pairing/              # DM 发送者审批
│   ├── skills/               # 内置技能定义
│   ├── security/             # 工作区限制、SSRF 防护、沙箱
│   ├── sdk/                  # Python SDK（程序化调用）
│   ├── triggers/             # 本地触发器
│   └── utils/                # 工具函数
├── webui/                    # 🎨 React 前端
├── tests/                    # 测试（镜像 nanobot/ 结构）
├── docs/                     # 文档
├── .agent/                   # 设计约束 / 安全边界 / 常见陷阱
├── AGENTS.md                 # AI 编码指南（架构概览在此）
├── CONTRIBUTING.md           # 贡献流程与 PR 规范
└── README.md                 # 项目入口
```

---

## 5. 全链路阅读顺序

**不要分阶段。** 跟着下面这条链从头读到尾，等于把一条"用户发消息 → bot 回复"的完整路径走一遍。每一站给出：文件、要重点读的方法、以及我在代码里加的中文注释热点（标 📝）。

> 先读两份纯文档打底（30 分钟）：[README.md](README.md) → [AGENTS.md](AGENTS.md) → `.agent/design.md` + `gotchas.md`。然后开始走代码链。

```
用户发消息
   │
   ▼  ❶ 启动入口
   ▼  ❷ 消息接入 (Channel)
   ▼  ❸ 消息总线 (MessageBus)
   ▼  ❹ 编排核心 (AgentLoop)
   ▼  ❺ 对话循环 (AgentRunner)  ← loop→runner 交接
   ▼  ❻ LLM 供应商 (Provider)
   ▼  ❼ 工具执行 (Tool)
   ▼  ❽ 会话存储 (Session)
   ▼  ❾ 回复返回 (Outbound → Channel)
```

### ❶ 启动入口

| 文件 | 重点读 |
|------|--------|
| [nanobot/cli/commands.py](nanobot/cli/commands.py) | `gateway` 命令如何拉起 Gateway + AgentLoop |

理解：`nanobot gateway` 这个命令最终在这里启动了 HTTP/WebSocket 服务，并实例化 `AgentLoop`。

### ❷ 消息接入 — Channel

| 文件 | 重点读 |
|------|--------|
| [nanobot/channels/base.py](nanobot/channels/base.py) | `Channel` 基类：定义 `receive` / `send` 接口 |
| [nanobot/channels/telegram.py](nanobot/channels/telegram.py)（或 [websocket.py](nanobot/channels/websocket.py)） | 一个具体实现：平台消息如何转成 `InboundMessage` |
| [nanobot/channels/manager.py](nanobot/channels/manager.py) | Channel 的自动发现与协调 |

理解：外部平台消息进来，被包装成 `InboundMessage` 事件，准备丢给总线。

### ❸ 消息总线 — MessageBus

| 文件 | 重点读 |
|------|--------|
| [nanobot/bus/queue.py](nanobot/bus/queue.py) | `MessageBus`、`InboundMessage` / `OutboundMessage`、`publish` / `consume_inbound` |
| [nanobot/bus/events.py](nanobot/bus/events.py) | 事件类型定义 |

理解：总线是 Channel 和 Agent 之间的解耦层。Channel 只管 `publish`，AgentLoop 只管 `consume`。

### ❹ 编排核心 — AgentLoop（🔥 最重要）

| 文件 | 重点读 | 注释 |
|------|--------|------|
| [nanobot/agent/loop.py](nanobot/agent/loop.py) | `AgentLoop` 类 | |
| 同上 | `run()` —— 从总线消费消息、路由（优先命令/中途注入/新任务） | |
| 同上 | `_dispatch()` —— 会话锁（同会话串行、跨会话并发）、建流式回调 | 📝 |
| 同上 | `_process_message()` —— 构造 `TurnContext`，**状态机驱动** | 📝 |
| 同上 | `_TRANSITIONS` 表 —— 状态转移主干 | 📝 |
| 同上 | `_state_restore` → `_state_compact` → `_state_command` → `_state_build` → `_state_run` → `_state_save` → `_state_respond` —— 七个状态方法，每个看一句话注释就知道它在链路里干嘛 | 📝 |

理解：这是整条链的"大脑"。一条消息被状态机推过 7 个状态：恢复检查点 → 压缩历史 → 判命令 → 组装消息 → 跑 LLM → 存历史 → 组装回复。**`_state_run` 里调用 `self.runner.run(...)` 就是交接点** 📝。

### ❺ 对话循环 — AgentRunner（🔥 最重要）

| 文件 | 重点读 | 注释 |
|------|--------|------|
| [nanobot/agent/runner.py](nanobot/agent/runner.py) | `AgentRunSpec`（输入包）/ `AgentRunResult`（输出） | 📝 |
| 同上 | `AgentRunner` 类 | 📝 |
| 同上 | `run()` —— 公共入口，套 hook + 异常处理 | 📝 |
| 同上 | `_run_core()` —— **真正的多轮对话循环**，顶部有 4 步伪代码注释 | 📝 |
| 同上 | `_request_model()` —— 调一次 LLM | 📝 |
| 同上 | `_execute_tools()` / `_run_tool()` —— 执行工具 | 📝 |

理解：`_run_core` 是全项目的心脏。循环逻辑：**为模型准备消息 → 调 LLM → 有工具调用就执行并 continue → 没有就收尾 break**。读 `_run_core` 顶部那段中文注释就能掌握全局。

### ❻ LLM 供应商 — Provider

| 文件 | 重点读 |
|------|--------|
| [nanobot/providers/base.py](nanobot/providers/base.py) | `LLMProvider` 基类：`send` / `LLMResponse` / `ToolCallRequest` 接口 |
| [nanobot/providers/factory.py](nanobot/providers/factory.py) | 如何按 model 名/配置创建 provider（路由 + key 校验） |
| [nanobot/providers/openai_compat_provider.py](nanobot/providers/openai_compat_provider.py)（或 [anthropic_provider.py](nanobot/providers/anthropic_provider.py)） | 一个具体实现：怎么把请求发出去、把流式回复接回来 |
| [nanobot/providers/registry.py](nanobot/providers/registry.py) | provider 注册表、模型名 → provider 的匹配规则 |

理解：`_request_model` 最终调到这里。这一层抽象让你能换任意 LLM 而不动上层循环。

### ❼ 工具执行 — Tool

| 文件 | 重点读 |
|------|--------|
| [nanobot/agent/tools/registry.py](nanobot/agent/tools/registry.py) | `ToolRegistry`：工具注册、`get_definitions`（喂给 LLM 的 schema） |
| [nanobot/agent/tools/base.py](nanobot/agent/tools/base.py) | 工具基类 |
| [nanobot/agent/tools/filesystem.py](nanobot/agent/tools/filesystem.py)（或 [shell.py](nanobot/agent/tools/shell.py)） | 一个具体工具：参数校验、执行、返回结果 |
| [nanobot/agent/tools/sandbox.py](nanobot/agent/tools/sandbox.py) | Shell 沙箱（安全边界） |

理解：`_run_tool` 调到这里。工具返回的结果会被 append 回 messages，进入下一轮 LLM 调用。

### ❽ 会话存储 — Session

| 文件 | 重点读 |
|------|--------|
| [nanobot/session/manager.py](nanobot/session/manager.py) | `Session` 类、`history.jsonl` 的**原子写**（temp+fsync+rename） |
| [nanobot/agent/autocompact.py](nanobot/agent/autocompact.py) | 上下文自动压缩（历史太长时归档） |

理解：`_state_save` 调到这里。一轮对话的完整消息列表（含工具结果）落盘，下次对话从这里恢复。

### ❾ 回复返回 — Outbound → Channel

回到 [loop.py](nanobot/agent/loop.py) 的 `_state_respond` 📝：把最终回复组装成 `OutboundMessage` → 经 [bus/queue.py](nanobot/bus/queue.py) 的 `publish_outbound` → 到对应 Channel 的 `send` → 发回聊天平台。

**至此一条消息的完整生命周期走完。**

---

## 6. 读完全链路后，挑一个子系统深入

走完上面 ❶→❾ 后，按兴趣挑下面任一子系统精读（每条都是"基类→具体实现→注册/工厂"三件套）：

| 子系统 | 阅读路径 |
|--------|---------|
| Provider 体系 | [base.py](nanobot/providers/base.py) → [openai_compat_provider.py](nanobot/providers/openai_compat_provider.py) → [factory.py](nanobot/providers/factory.py) → [registry.py](nanobot/providers/registry.py) |
| Tool 体系 | [tools/base.py](nanobot/agent/tools/base.py) → [registry.py](nanobot/agent/tools/registry.py) → [filesystem.py](nanobot/agent/tools/filesystem.py) → [shell.py](nanobot/agent/tools/shell.py) → [sandbox.py](nanobot/agent/tools/sandbox.py) |
| Session 管理 | [session/manager.py](nanobot/session/manager.py) → [agent/autocompact.py](nanobot/agent/autocompact.py) |
| Memory | [agent/memory.py](nanobot/agent/memory.py)（Dream 两阶段整合） |
| Channel 体系 | [base.py](nanobot/channels/base.py) → [telegram.py](nanobot/channels/telegram.py) → [manager.py](nanobot/channels/manager.py) |
| 前端 WebUI | [webui/vite.config.ts](webui/vite.config.ts) → [webui/src/](webui/src/)（WebSocket 多路复用协议连 Gateway） |

进阶话题（按需）：子代理 [subagent.py](nanobot/agent/subagent.py) ｜ 长期目标 [goal_state.py](nanobot/session/goal_state.py) ｜ MCP [tools/mcp.py](nanobot/agent/tools/mcp.py) ｜ 定时 [cron_turns.py](nanobot/agent/cron_turns.py) ｜ Python SDK [nanobot/nanobot.py](nanobot/nanobot.py)。

---

## 7. 实践项目建议

读完全链路后，挑一个练手（由易到难）：

| 难度 | 项目 | 涉及模块 |
|------|------|---------|
| ⭐⭐ | 对接一个新 Provider（如 DeepSeek / Groq） | Provider |
| ⭐⭐ | 改系统提示词塑造 bot 人格 | Context |
| ⭐⭐⭐ | 接一个新 Channel（如钉钉机器人） | Channel |
| ⭐⭐⭐ | 实现每日定时天气播报 | Cron + Tool |
| ⭐⭐⭐⭐ | 写一个 MCP Server 并接入 nanobot | MCP + Tool |

---

## 8. 调试技巧

```bash
# 项目用 loguru，终端直接看日志。任意模块加：
from loguru import logger
logger.info("调试: {}", some_variable)

# 只看关键链路日志，过滤噪音：
nanobot gateway 2>&1 | grep -E "(AgentLoop|AgentRunner|ERROR)"

# VSCode 单步调试：在 .vscode/launch.json 配置
#   { "name": "nanobot gateway", "type": "python", "request": "launch",
#     "module": "nanobot.cli.commands", "args": ["gateway"] }
```

> 💡 **WebUI 没反应时，第一时间看 gateway 终端日志**——所有错误（API key 缺失、模型名不对、接口 401 等）都打到那里了，比 UI 上猜直接得多。

---

## 9. 常见陷阱（来自 `.agent/gotchas.md`）

- ❌ 不要全项目跑 `ruff format`，会破坏 git blame；只用 `ruff check`，格式化限定在你改动的文件。
- ⚙️ `config.json` 里的 `${VAR}` 环境变量在**加载时**解析，缺变量会抛 `ValueError`。
- 🪟 Windows 必须用 `pathlib.Path`；`ExecTool` 用 `cmd /c`，stdout 强制 UTF-8。
- 📝 模板在 [nanobot/templates/](nanobot/templates/) 是 Jinja2 markdown，改它们 = 改 Agent 行为。
- 🔒 Session 写 `history.jsonl` 必须**原子写**（temp + fsync + rename）。
- 🧠 上下文污染：memory / 历史 / 提示输入里的任何内容都可能被重放进后续 LLM 调用。
- ⚠️ `loop.py` / `runner.py` 是 `.agent/design.md` 标记的"核心路径，尽量别动"——本地学习加注释可以，但**别把注释提交进 PR**。

---

## 10. 资源

| 资源 | 链接 |
|------|------|
| 官方文档 | [nanobot.wiki](https://nanobot.wiki) |
| GitHub | [HKUDS/nanobot](https://github.com/HKUDS/nanobot) |
| Discord 社区 | [discord.gg/MnCvHqpUGB](https://discord.gg/MnCvHqpUGB) |
| PyPI | [nanobot-ai](https://pypi.org/project/nanobot-ai/) |
| 设计原则 | `.agent/design.md` |
| 安全边界 | `.agent/security.md` |
| 常见陷阱 | `.agent/gotchas.md` |

---

> **一句话总结**：nanobot 的核心是一条消息循环链——**Channel 收 → Bus 传 → Loop 编排 → Runner 跑对话 → Provider 调模型 → Tool 执行 → Session 存 → Channel 回**。按 §5 的 ❶→❾ 顺序读一遍，这条链就通了。🚀
